title: "⏳ ui: show expiry countdown"
description: |
  Show an EXPIRES_IN countdown in the active widget renderer and hide it once
  the server confirms `expiry_disabled=true` via the /touch endpoint response.
  Also add a small reducer to patch requests by id.

  Tests:
  - pnpm -C agent-ui-system run check
