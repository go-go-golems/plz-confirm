title: "📝 docs: record completion action suppression"
description: |
  Update ticket `001-QOL-HISTORY-TUI` diary/changelog with the id-based completion
  correlation change (so DevTools doesn’t show duplicate completion actions).

  Tests:
  - N/A (docs only)
