title: "🐛 fix: dedupe UI history + queue requests"
description: |
  Fix duplicate entries in the web UI request history by making completion updates
  idempotent by request id and by routing WS `request_completed` through the same
  completion reducer.

  Tests:
  - make ci
