title: "💄 ui: show expiry progress bar"
description: |
  Add a small progress bar under the EXPIRES_IN label so the expiry countdown
  is easier to scan at a glance.

  Tests:
  - pnpm -C agent-ui-system run check
