title: "📝 docs: add ws watcher usage"
description: |
  Record the new `plz-confirm ws` command in the ticket diary, including how
  to use it to observe timeout events over WebSocket.

  Tests:
  - N/A (docs only)
