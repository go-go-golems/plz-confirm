title: "🧹 ui: suppress duplicate completion actions"
description: |
  Prevent duplicate Redux completion actions by correlating completion events by
  request id: local submit and WS echo both funnel through a single dispatch path.

  Tests:
  - pnpm -C agent-ui-system run check
