title: "🧹 chore: ignore prescribe session files"
description: |
  Add `.pr-builder/` to `.gitignore` so local prescribe sessions and generated PR context aren’t accidentally committed.

  Tests:
  - N/A
