title: "📝 docs: add prescribe PR playbook"
description: |
  Add a ticket-local playbook documenting how to create a PR using `prescribe`
  (including recommended filters and `PINOCCHIO_PROFILE=gemini-2.5-pro`).

  Tests:
  - N/A (docs only)
