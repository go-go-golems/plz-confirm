title: "⏱️  cli: handle timeout + session-id"
description: |
  Add `--session-id` to widget commands so they can target the right WebSocket
  session, and handle server-side timeouts (`status=timeout`) without panicking.

  Tests:
  - go test ./... -count=1
  - pnpm -C agent-ui-system run check
