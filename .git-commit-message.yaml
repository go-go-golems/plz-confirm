title: "📝 docs: record expiresAt enforcement"
description: |
  Update ticket `001-QOL-HISTORY-TUI` docs to record the server-side `expiresAt`
  enforcement and how it’s broadcast/displayed.

  Tests:
  - N/A (docs only)
