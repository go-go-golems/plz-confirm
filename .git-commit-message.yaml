title: "📝 docs: update ticket 001 history fix"
description: |
  Update ticket `001-QOL-HISTORY-TUI` docs (index/tasks/changelog/diary) to record
  the history de-dupe + pending queue fix and how to reproduce it.

  Tests:
  - N/A (docs only)
