title: "✨ cli: add ws event watcher command"
description: |
  Add `plz-confirm ws` to connect to `/ws?sessionId=...` and print events, so
  we can test timeout behavior over WebSocket from the CLI.

  Tests:
  - go test ./... -count=1
