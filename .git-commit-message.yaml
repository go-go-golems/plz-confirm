title: "📝 docs: add WS/REST protocol analysis and expiresAt tasks"
description: |
  Added a protocol analysis document covering WebSocket vs REST and how the CLI and UI interact with the backend.

  Added ticket tasks for making `expiresAt` an enforceable UI timeout (countdown + stop-on-interaction) and for a default timeout response.

  Tests:
  - Not run (docs-only change)
