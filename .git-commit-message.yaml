title: "📝 docs: close pre-2026 tickets"
description: |
  Close (set status to `closed`) all tickets with paths before `2026/01/03`, even if they still have open tasks, as part of ticket hygiene.

  Tests:
  - N/A (docmgr bookkeeping only)
