title: "👷 ci: ignore generated files in gosec"
description: |
  Configure gosec to skip generated Go files (e.g. protobuf `*.pb.go`) so CI
  doesn’t fail on generated unsafe usage.

  Tests:
  - make gosec
