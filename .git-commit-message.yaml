title: "✨ server: scope WS by sessionId"
description: |
  Implement real session handling on the Go server: WebSocket connections
  subscribe to a `sessionId`, pending replay is filtered by sessionId, and
  request events are broadcast only to that session.

  Tests:
  - go test ./... -count=1
