title: "📝 docs: record history fix validation"
description: |
  Update ticket `001-QOL-HISTORY-TUI` docs (diary/changelog) with validation notes
  from running the dev UI and confirming the history no longer duplicates.

  Tests:
  - N/A (docs only)
