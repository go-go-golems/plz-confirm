title: "📝 docs: close request metadata ticket"
description: |
  Update ticket `003-REQUEST-METADATA` docs (diary/index/changelog) to reflect the
  completed implementation and close the ticket.

  Tests:
  - N/A (docs only)
