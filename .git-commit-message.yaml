title: "🧹 cleanup: delete legacy Node server entry"
description: |
  Removed the legacy `agent-ui-system/server/index.ts` Node/Express backend entry point from git (the UI now only ships as a Vite build and is served/embedded by the Go server).

  Tests:
  - Not rerun (already passing in previous commit)
