title: "📝 docs: add tmux timeout+ws demo script"
description: |
  Add a ticket-local tmux script to run backend+vite+ws watcher and trigger a
  short-timeout request, and document the workflow in the diary.

  Tests:
  - N/A (docs only)
