title: "📝 docs: record sessionId scoping"
description: |
  Update ticket `002-REMOVE-LEGACY-REST-JSON` docs to record that the Go server
  now scopes WebSocket connections and broadcasts by `sessionId`.

  Tests:
  - N/A (docs only)
