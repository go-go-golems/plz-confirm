title: "🐛 ui: fix WidgetRenderer hook order"
description: |
  Fix a Rules of Hooks violation in WidgetRenderer by ensuring useEffect is
  called unconditionally, and increase the CLI default --wait-timeout to 300s.

  Tests:
  - go test ./... -count=1
  - pnpm -C agent-ui-system run check
