# Tasks

## TODO

- [x] Create ticket + docs (bug report, analysis, diary)
- [x] Trace schema/context flow (CLI → server → WS → React)
- [ ] Implement UI rendering of schema context in `FormDialog.tsx` (schema description + field title/description)
- [ ] Decide on “instructions” support: schema `description` only vs add `FormInput.message` + CLI flag
- [ ] Add a minimal UI regression test / manual test playbook for quiz-style schemas

